# bradfordwagner.ansible-role-argo

## Updating the version

Version is controlled by `argo_version` in `defaults/main.yml`.

Check for the latest release at: https://github.com/argoproj/argo-workflows/releases

```yaml
argo_version: vX.Y.Z
```
